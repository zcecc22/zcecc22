server {
  listen 80 default_server;
  listen [::]:80 default_server ipv6only=on;
  
  location /.well-known {
    alias /var/www/nodex.frentzel.eu/.well-known;
  }
  
  location / {
    return 301 https://$host$request_uri;
  }
}
